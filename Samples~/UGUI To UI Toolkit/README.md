# UGUI To UI Toolkit Sample

1. `UIDocument` を scene に置き、終点にしたい要素へ name を付ける
2. `UiFlightSampleUGuiToUiToolkit` に source の `Image` と `UIDocument` を割り当てる
3. `targetElementName` を一致させ、button から `Play()` を呼ぶ
