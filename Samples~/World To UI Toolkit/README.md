# World To UI Toolkit Sample

1. pickup 役の `Transform` と camera を用意する
2. `UIDocument` 側の終点 `VisualElement` 名を `targetElementName` に合わせる
3. `sprite` と optional な `UiFlightBezierProfile` を割り当てて `Play()` を呼ぶ
